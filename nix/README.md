This directory contains all the Nix code to extend nixpkgs with our own packages.

First study `./overlay.nix`. It's imported from <nixtodo/default.nix>
and imports all other files in this directory.
